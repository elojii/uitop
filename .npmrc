# Vercel's @vercel/backends builder only recreates some of pnpm's node_modules
# symlinks when packaging a service, so symlinked deps (e.g. @nestjs/typeorm)
# resolve to nothing at runtime. A hoisted, symlink-free tree avoids that.
node-linker=hoisted
